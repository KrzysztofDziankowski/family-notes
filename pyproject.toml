[project]
name = "family-notes"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "django>=5.2,<5.3",
    "django-allauth[socialaccount]>=65.19,<66",
    "gunicorn>=26.2.0",
    "psycopg[binary]>=3.3.6",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.1",
]
